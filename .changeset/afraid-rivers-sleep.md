---
'@nike-release-checker/sdk': minor
---

Add per-model image URLs to formatted product feed releases by mapping published content nodes to models via product ID, while keeping the release cover image as a fallback.
